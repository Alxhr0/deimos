# Template file for 'bootc'
pkgname=bootc
version=1.16.9
revision=1
build_style=gnu-make
make_install_target="bin install-all PREFIX=/usr"
hostmakedepends="composefs ostree libxslt pkg-config"
makedepends="cargo libcomposefs-devel make libostree-devel openssl-devel fuse3-devel"
checkdepends="attr-progs cpio elfutils gnupg python3-yaml tar which xz jq"
short_desc="Boot and upgrade via container images"
maintainer="Alxhr0"
license='Apache-2.0 OR MIT'
homepage="https://github.com/bootc-dev/bootc"
changelog="https://github.com/bootc-dev/bootc/release"
distfiles="https://github.com/bootc-dev/bootc/archive/refs/tags/v${version}.tar.gz"
checksum=207384deb196198ac4764c5b42bb558f7c661494302b380afc09447678538386

do_install() {
	make bin install-all PREFIX=/usr
}